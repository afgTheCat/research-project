[build-system]
requires = ["maturin>=0.13,<0.14"]
build-backend = "maturin"

[project]
name = "resframe"
requires-python = ">=3.7"
classifiers = []

[tool.maturin]
manifest-path = "crates/reservoire-extensions/Cargo.toml"
python-source = "python"

